import React from 'react';

const Footer = ({name}) => {
    return (
        <div className='bg-amber-400 text-center'>
            <h3>{name}</h3>
        </div>
    );
} 

export default Footer;<h3>{name}</h3>